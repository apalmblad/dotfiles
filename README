The dotfiles of Adam Palmblad, mostly vim related.

Setup:
1. mkdir -p ~/.dotfiles; git clone REPO ~/.dotfiles
1. Install homebrew, via https://brew.sh/
1. brew bundle
3. npm -g i sass-lint eslint
2. rcup
1. ln -s /usr/local/bin/ggrep /usr/local/bin/grep
1. Apps: `mas install 585829637` # todoist
1. Apps: `mas install 1232780281` # notion
1. Apps: `mas install 441258766` # magnet
1. sh -c "$(curl -fsSL https://raw.github.com/ohmyzsh/ohmyzsh/master/tools/install.sh)" # oh-my-zsh


